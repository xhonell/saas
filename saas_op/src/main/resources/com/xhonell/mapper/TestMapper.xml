<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 命名空间，用于隔离sql，后面还有一个很重要的作用 -->
<mapper namespace="com.xhonell.mapper.TestMapper">

    <!-- 查询所有 -->
    <select id="selectAll" resultType="com.xhonell.bean.Item">
        select * from item;
    </select>

</mapper>